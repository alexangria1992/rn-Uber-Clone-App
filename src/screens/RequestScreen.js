import React from "react";
import { StyleSheet, Text, View } from "react-native";

export default function RequestScreen() {
  return <Text>RequestScreen</Text>;
}

const styles = StyleSheet.create({});
